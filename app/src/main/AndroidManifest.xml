<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ruzun.betaqti">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo1212"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Setting"></activity>
        <activity
            android:name=".Appointments"
            android:label="خدمات > مواعيدي"></activity>
        <activity
            android:name=".UserAppoints"
            android:label="مواعيد" />
        <activity
            android:name=".UserRequest"
            android:label="خدمات > سجل طلباتي" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".HomeActivity"
            android:label="خدمات" />
        <activity
            android:name=".LogInActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MessageActivity" />
        <activity
            android:name=".EditFile"
            android:label="خدمات > تعديل الملف الشخصي" />
        <activity
            android:name=".ViewProfile"
            android:label="خدمات > استعراض الملف الشخصي" />
        <activity
            android:name=".Records"
            android:label="تحرير الطلبات" />
    </application>
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>